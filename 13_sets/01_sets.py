# A set is a collection type that is:

# Unique (i.e. no duplicate elements)
# Unordered (i.e. no specific order of elements)
# Mutable (i.e. we can add or remove new items from the set if we want)

s = {3, 2, 23, 14}

print(s, type(s))
print(s[3]) # You are not allowed to do something like this.